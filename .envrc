strict_env
nix_direnv_disallow_fallback
while IFS= read -r -d "" path; do
  watch_file "$path"
done < <(find modules/maintenance packages/mcp-dotnix -print0)
use flake . --no-update-lock-file --no-write-lock-file
